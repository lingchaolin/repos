<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<properties resource="jdbc.properties"/>
	<environments default="development">
		<environment id="development">
			<tractionManager type="JDBC"></tractionManager>
			<dataSource type="POOLED">
				<properties name="driver" value="${driver}"></properties>
				<properties name="url" value="${url}"></properties>
				<properties name="username" value="${username}"></properties>
				<properties name="password" value="${password}"></properties>
			</dataSource>
		</environment>
	</environments>
	<mappers>
		<mapper class="com.aowin.dao.PersonMapper" />
	</mappers>
</configuration>