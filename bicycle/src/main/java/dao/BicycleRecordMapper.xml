<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.BicycleRecordMapper">

    <insert id="addBicycleRecord" parameterType="model.BicycleRecord">
        <selectKey keyProperty="recordId" resultType="int" order="BEFORE">
            select SEQ_LOG.nextval recordId from dual
        </selectKey>
        insert into bicycle_record values(#{recordId},#{bicycleId},#{cardId},#{rentTime},#{rentPileId},#{returnTime,jdbcType=NUMERIC}
        ,#{returnPileId,jdbcType=NUMERIC},#{money,jdbcType=NUMERIC},#{remark,jdbcType=VARCHAR})
    </insert>
</mapper>